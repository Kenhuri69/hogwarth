# Dépendances Python des outils du jeu Poudlard & Magie.
# Installation :  python3 -m pip install -r tools/requirements.txt
#
# ⚠️ cairosvg nécessite la lib système cairo :
#     Debian/Ubuntu : sudo apt-get install -y libcairo2
#     macOS         : brew install cairo pango

# --- Pipeline d'icônes d'items (tools/icon_factory.py) ---
pillow
cairosvg
numpy
scipy

# --- Détourage optionnel (rembg) — LOURD, À LA DEMANDE ---
# Utilisé par tools/process_monster_png.py ET, en fallback fond chargé, par
# tools/sheet_extract.py (--method rembg). Modèle U²-Net ~176 Mo au 1er run.
# Pour nos planches d'icônes à FOND PLAT CLAIR, rembg est inutile (le détourage
# par remplissage de sheet_extract suffit et est plus net). Installer au besoin :
#     python3 -m pip install "rembg[cpu]"
# rembg
