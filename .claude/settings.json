{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/ensure-test-deps.sh\"",
            "timeout": 600,
            "statusMessage": "Vérif des dépendances de test (Playwright)…"
          }
        ]
      }
    ]
  }
}
